public enum PoolType
{
    GameObject,
    SFX,
    VFX
}

public enum PlayerStateType
{
    Movement = 0,
    Dash,
    Teleportation,
}

public enum ShapeType
{
    Circle = 1,
    Triangle,
    Square,
}